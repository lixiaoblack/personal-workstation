# Personal Workstation

一款基于 Electron + React 的智能桌面助手，集成 AI 对话、知识库管理、文件智能解析等功能。

## 功能特性

- 🤖 **AI 智能对话** - 支持多模型接入（OpenAI、百炼、智谱、Ollama），流式输出，Agent 模式
- 📚 **知识库管理** - RAG 检索增强，支持多格式文档上传，智能语义搜索
- 📄 **文件智能解析** - 支持 PDF（含扫描版 OCR）、Word、PPT、Excel 等格式
- 🔍 **联网搜索** - DuckDuckGo 搜索集成，实时获取网络信息
- 🎨 **主题切换** - 支持浅色/深色/跟随系统三种主题模式
- 💾 **本地数据存储** - SQLite 本地数据库，数据安全可控

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Electron 主进程                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ 窗口管理     │  │ IPC 通信    │  │ WebSocket Server    │  │
│  │             │  │             │  │   (ws://localhost)  │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                     渲染进程 (React)                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ AI Chat     │  │ Knowledge   │  │ Settings            │  │
│  │ 对话界面    │  │ 知识库管理  │  │ 设置页面            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    Python 服务 (可选)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ LangGraph   │  │ RAG 检索    │  │ PaddleOCR           │  │
│  │ Agent 框架  │  │ 向量存储    │  │ 文字识别            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 技术栈

| 层级 | 技术 |
|------|------|
| 桌面框架 | Electron |
| 前端框架 | React 18 + TypeScript |
| UI 组件 | Ant Design 5 + Ant Design X |
| 样式方案 | Tailwind CSS + SCSS |
| 构建工具 | Vite 5 |
| 包管理器 | pnpm 9 |
| 本地数据库 | SQLite (better-sqlite3) |
| AI 框架 | LangGraph (Python) |
| 向量数据库 | LanceDB |
| OCR 引擎 | PaddleOCR |

## 下载安装

前往 [Releases](https://github.com/lixiaoblack/personal-workstation/releases) 页面下载最新版本：

| 平台 | 文件格式 | 说明 |
|------|----------|------|
| macOS (Apple Silicon) | `.dmg` | 适用于 M1/M2/M3 芯片 Mac |
| Windows | `.exe` | 安装版或便携版 |

## 开发指南

### 环境要求

- Node.js >= 18
- pnpm >= 9
- Python >= 3.11 (可选，用于 AI Agent 功能)

### 安装依赖

```bash
# 安装 Node.js 依赖
pnpm install

# 安装 Python 依赖 (可选)
pip install -r python-service/requirements.txt
```

### 开发模式

```bash
pnpm dev
```

### 构建打包

```bash
pnpm build
```

## 项目结构

```
personal-workstation/
├── electron/                 # Electron 主进程
│   ├── main.ts              # 主进程入口
│   ├── preload.ts           # 预加载脚本
│   ├── database/            # 数据库服务
│   └── services/            # 后端服务
├── src/                     # 渲染进程 (React)
│   ├── components/          # 全局组件
│   ├── views/               # 页面视图
│   ├── hooks/               # 自定义 Hooks
│   ├── contexts/            # React Context
│   ├── stores/              # 状态管理
│   └── types/               # 类型定义
├── python-service/          # Python AI 服务
│   ├── agent/               # LangGraph Agent
│   ├── rag/                 # RAG 检索模块
│   └── skills/              # 技能模块
└── resources/               # 应用资源
```

## 许可证

MIT License
