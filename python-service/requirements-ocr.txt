# OCR 模块专用依赖
# 用于 PyInstaller 打包 OCR 模块

# ==================== HTTP 服务 ====================
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart

# ==================== OCR 核心 ====================
# 注意：PaddleOCR 会尝试安装 PyMuPDF，但 OCR 模块不需要
# 安装顺序很重要：先安装其他依赖，再安装 PaddleOCR
paddlepaddle>=2.5.0
Pillow>=10.0.0
opencv-python-headless
numpy

# PaddleOCR 最后安装，跳过 PyMuPDF 依赖
# pip install paddleocr==2.6.1.3 --no-deps
paddleocr==2.6.1.3
