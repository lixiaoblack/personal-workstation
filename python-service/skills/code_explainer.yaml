# 示例：自定义技能配置
# 这是一个演示如何通过 YAML 文件定义技能的示例

# 技能元数据
metadata:
  name: code_explainer
  display_name: 代码解释器
  description: 解释代码的功能和逻辑，帮助理解复杂代码
  author: user
  version: 1.0.0
  tags:
    - 代码
    - 解释
    - 分析
  icon: 💻

# 技能类型
# builtin: 内置技能（代码实现）
# custom: 自定义技能（配置文件）
# composite: 组合技能
type: custom

# 触发方式
# manual: 手动调用
# keyword: 关键词触发
# intent: 意图识别触发
trigger: intent

# 触发关键词（当 trigger=keyword 时使用）
trigger_keywords:
  - 解释代码
  - 代码是什么意思
  - 这段代码

# 绑定的工具
# 定义技能使用哪些工具以及参数映射
tools:
  - tool_name: echo
    parameter_mapping:
      message: $code
    output_key: code_echo

# 提示词模板
# 支持 {param} 格式的参数插值
prompt_template: |
  请解释以下代码的功能和逻辑：

  ```
  {code}
  ```

  请用简洁易懂的语言说明：
  1. 这段代码的整体功能是什么？
  2. 主要的执行步骤是什么？
  3. 有什么需要注意的地方？

# 参数定义
parameters:
  code:
    type: string
    description: 要解释的代码片段
    required: true

# 使用示例
examples:
  - "解释这段代码：def hello(): print('world')"
  - "帮我理解这个函数的作用"

# 执行超时（秒）
timeout: 30
